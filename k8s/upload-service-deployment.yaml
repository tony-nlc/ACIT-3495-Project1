apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-service
  namespace: video-streaming
spec:
  replicas: 2
  selector:
    matchLabels:
      app: upload-service
  template:
    metadata:
      labels:
        app: upload-service
    spec:
      containers:
      - name: upload-service
        image: upload-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5003
        env:
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: JWT_SECRET_KEY
        - name: PORT
          value: "5003"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: video-streaming
spec:
  type: ClusterIP
  selector:
    app: upload-service
  ports:
  - port: 5003
    targetPort: 5003
    name: http